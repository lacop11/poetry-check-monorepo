# ⚠️ UNMAINTAINED ⚠️
This repo is dead and uses old poetry version. I won't be updating it.

If you found it useful it would be pretty easy to fork and upgrade to newer poetry, but I recommend just switching to `uv` instead.

# `poetry check` for monorepos

Like the `poetry-check` pre-commit hook but executed on each directory separately to support repos with multiple independent `pyproject.toml` files.
